{
  "mcpServers": {
    "omh-memory": {
      "command": "bash",
      "args": [
        "-c",
        "if [ -n \"$CLAUDE_PLUGIN_ROOT\" ]; then cd \"$CLAUDE_PLUGIN_ROOT\" || exit 1; fi; exec bash bin/omh-memory.sh"
      ],
      "cwd": "."
    }
  }
}
